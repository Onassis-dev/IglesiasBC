---
import H2 from "../basics/H2.astro";
import Section1 from "../basics/Section1.astro";

interface Props {
  mission?: string;
  animations: number;
  language: string;
}

const { mission, animations, language } = Astro.props;
---

<Section1 animations={animations}>
  <H2 className="text-center"
    >{language === "es" ? "Nuestra Misión" : "Our Mission"}</H2
  >
  <p class="text-lg mb-4 w-full m-0 p-0 mt-2 text-center max-w-4xl mx-auto">
    {mission}
  </p>
</Section1>
