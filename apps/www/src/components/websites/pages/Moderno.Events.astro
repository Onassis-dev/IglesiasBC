---
import type { Website } from "@/components/pages/Website/website.types";
import EventsGallery1 from "../components/EventsGallery1.astro";

interface Props {
    webpage: Website;
}

const { webpage } = Astro.props;
---

<EventsGallery1 language={webpage.language} pageTitle={webpage.title} events={webpage.events} animations={webpage.animations} />
